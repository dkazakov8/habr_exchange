import { hot } from 'react-hot-loader/root';
import React from 'react';
import { RouterConnected } from 'components/Router';

function App() {
  return <RouterConnected />;
}

export default hot(App);
